# Cloudflare AI Gateway
CLOUDFLARE_GATEWAY_URL=https://gateway.ai.cloudflare.com/v1/{account_id}/{gateway_name}/openai
CLOUDFLARE_GATEWAY_TOKEN=your-cloudflare-ai-gateway-token
# You may also set a compat chat endpoint such as:
# CLOUDFLARE_GATEWAY_URL=https://gateway.ai.cloudflare.com/v1/{account_id}/{gateway_name}/compat/chat/completions
# The app will normalize chat/embeddings routes to avoid malformed URLs.

# Postgres — on Railway, set one of these reference variables:
#   DATABASE_URL=${{pgvector.DATABASE_URL}}
#   DATABASE_PRIVATE_URL=${{pgvector.DATABASE_PRIVATE_URL}}
DATABASE_URL=postgresql://postgres:password@localhost:5432/postgres

# NewsBlur for RSS feed ingestion
NEWSBLUR_USERNAME=your-newsblur-username
NEWSBLUR_PASSWORD=your-newsblur-password

# YouTube transcripts are fetched from https://defuddle.md/ and do not need an API key.

# Optional overlap windows for safer incremental ingest.
# Re-reads a recent slice on each run so late-arriving RSS stories or videos
# are deduped instead of being permanently skipped.
RSS_OVERLAP_SECONDS=172800
YOUTUBE_OVERLAP_SECONDS=172800

# Optional provider API keys for BYOK compat-routed providers.
# With Cloudflare AI Gateway Unified Billing, supported providers such as
# Anthropic do not need a provider API key in this app. Keep these empty unless
# you are intentionally using a BYOK provider path such as DeepSeek.
ANTHROPIC_API_KEY=
DEEPSEEK_API_KEY=
OPENAI_API_KEY=

# Optional GitHub publishing for generated reports.
# When set, report generation writes a Jekyll-style post to a fresh branch,
# opens a pull request against GITHUB_BRANCH, and can notify Discord.
# DISCORD_WEBHOOK_URL also enables Discord alerts for new Detect trends and
# detect-policy eval/optimize runs triggered via the Railway dashboard.
GITHUB_TOKEN=
GITHUB_REPO=kyleboas/research
GITHUB_BRANCH=main
DISCORD_WEBHOOK_URL=

# Optional model overrides (defaults come from config.json)
# Use exact provider-prefixed IDs; the app no longer rewrites aliases at runtime.
LEAD_MODEL=anthropic/claude-sonnet-4-6
MODEL=workers-ai/@cf/meta/llama-3.3-70b-instruct-fp8-fast
EMBED_MODEL=openai/text-embedding-3-small
SUMMARY_MODEL=anthropic/claude-sonnet-4-6
SYNTHESIS_MODEL=anthropic/claude-sonnet-4-6
REVISION_MODEL=anthropic/claude-sonnet-4-6

# For /compat routes, use provider-prefixed models (example above).
# For /openai routes, use OpenAI model names (for example: text-embedding-3-small).
